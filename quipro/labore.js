if (includeNonEnumerable) {
  // Code to be executed if includeNonEnumerable is true
  console.log("Include non-enumerable properties.");
} else {
  // Code to be executed if includeNonEnumerable is false
  console.log("Do not include non-enumerable properties.");
}
